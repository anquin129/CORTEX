{
  "builds": [
    { "src": "package.json", "use": "@vercel/next" }
  ],
  "ignoreCommand": "if [ "$VERCEL_GIT_COMMIT_REF" = "highlighting" ]; then exit 0; fi",
  "git": {
    "deploymentEnabled": {
      "highlighting": false
    }
  }
}
